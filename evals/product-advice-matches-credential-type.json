{
  "id": "product-advice-matches-credential-type",
  "promise": "A static-keys user is never told to run aws sso login.",
  "why": "The advice was hardcoded and sent key-pair users to a command that cannot help them.",
  "command": "scripts/test.sh --filter CredentialAdviceTests 2>&1 | grep -q 'with 0 failures'",
  "expect": "exit 0"
}
