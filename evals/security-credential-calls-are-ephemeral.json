{
  "id": "security-credential-calls-are-ephemeral",
  "promise": "No AWS call uses URLSession.shared, which carries a shared on-disk cache.",
  "why": "The SSO federation endpoint answers with live secret access keys.",
  "command": "! grep -rn 'URLSession\\.shared' app/Sources/HangarCore/ | grep -vE ':[[:space:]]*(//|/\\*|\\*)' | grep .",
  "expect": "exit 0"
}
